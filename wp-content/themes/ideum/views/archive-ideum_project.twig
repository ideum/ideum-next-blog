{% extends "base.twig" %}

{% block content %}

<!-- begin archive-ideum_project.twig -->

<section class="article-content">

  <div class="article-body">

    <section class="our-creative-services">

      <div class="section-info">

        {% if post.can_edit %}
        <a class="edit-link" href="{{post.edit_link}}">[Edit me!]</a>
        {% endif %}

        <h1>
          Our Creative Services
        </h1>

        <p>
          Lorem ipsum dolor sit amet, consectetur adipisicing elit. Tempore
          ipsum aperiam, non nihil voluptate eligendi, quas obcaecati voluptatum
          unde voluptas iusto optio omnis possimus? Placeat accusantium
          reprehenderit dicta rem? Illo!
        </p>

        <div class="select-box-wrap">

          <select ui-select2 ng-model="projectType" data-placeholder="Browse By Type">
            <option value=""></option>
          </select>
          <select ui-select2 ng-model="projectTopic" data-placeholder="Browse By Topic">
            <option value=""></option>
          </select>

        </div>

        <!-- Ben, I won't build the rest of this section out until everything actually works and the functionality is set in stone and complete. Also, remember, you said you were going to integrate the select box replacement script. When you do I will style. There is just no point until that is done. -->

      </div>

      <div class="custom-project-listing">

        {% for post in posts %}
        {% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
        {% endfor %}

      </div>

      <div class="tool-pagination">

      <div class="pagination-interior-wrap">

        {% if pagination.prev %}
          <a href="{{pagination.prev.link}}" alt="Previous" title="Previous" class="prev arrow arrow-left {{pagination.prev.link|length ? '' : 'invisible'}}"></a>
        {% endif %}
        <ul class="pages">
          {% for page in pagination.pages %}
            <li>
              {% if page.link %}
                <a href="{{page.link}}" class="{{page.class}}">{{page.title}}</a>
              {% else %}
                <span class="{{page.class}}">{{page.title}}</span>
              {% endif %}
            </li>
          {% endfor %}
        </ul>
        {% if pagination.next %}
          <a href="{{pagination.next.link}}" alt="Next" title="Next" class="next arrow arrow-right {{pagination.next.link|length ? '' : 'invisible'}}"></a>
        {% endif %}

      </div>

    </div>

    <!-- <div class="section-info">

      <p>&nbsp;</p>

      <p class=""><a href="#FIXME" class="button active">View All</a></p>

    </div> -->

  </section>

  <section class="clients-n-partners" ng-init="clientSliderIndex = 0">

    <div class="section-info">

      <h1>
        <a href="/client-list/">Clients &amp; Partners</a>
      </h1>

      <p>
        We have worked with top museums and cultural institutions, the world’s leading technology firms, along with exhibit design and fabrication firms on a variety of challenging and collaborative projects. 
      </p>

    </div>

    {% include 'client-slider.twig' %}

  </section>

  </div>

</section>

<!-- end archive-ideum_project.twig -->

{% endblock %}